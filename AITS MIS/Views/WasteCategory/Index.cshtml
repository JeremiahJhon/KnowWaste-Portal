
@{
    Layout = "~/Views/Shared/Shared.cshtml";
}

@using System.Data;

@{
    DataTable data = (DataTable)ViewBag.WasteCategory;
    <table>
        @{

            foreach (DataRow row in data.Rows)
            {
                <tr><td>@row["name"].ToString()</td></tr>
            }
        }
    </table>
}
